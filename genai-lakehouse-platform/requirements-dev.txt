# Local development + CI. Runtime dependencies live in pyproject.toml.
#
# pyspark and delta-spark are pinned to match Databricks Runtime 15.4 LTS
# (Spark 3.5.x / Delta 3.2.x). A local suite that passes on a different Spark
# minor than production is a test suite that lies to you.
pyspark==3.5.6
delta-spark==3.2.0

databricks-sdk>=0.29.0
databricks-vectorsearch>=0.40
databricks-sql-connector>=3.0.0

# Used by the bundle/entry-point consistency tests; without it that check
# silently skips instead of protecting the deploy.
pyyaml>=6.0
# tomllib is stdlib from 3.11; the 3.10 job needs the backport.
tomli>=2.0; python_version < "3.11"

pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
