name: CI

# NOTE: GitHub Actions only reads workflows from `.github/workflows/` at the
# repository ROOT. This project lives under OneDrive/Desktop/stock-valuation-predictor/,
# so every step sets `working-directory` to that subfolder.

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

env:
  PROJECT_DIR: OneDrive/Desktop/stock-valuation-predictor

jobs:
  smoke-test:
    # 3.13 is blocking; 3.14 is advisory. Streamlit Cloud runs 3.14, so a green
    # matrix that stopped at 3.12 was telling us nothing about production — but
    # 3.14 wheel availability for xgboost, shap and friends is upstream's
    # timetable, not ours, and a merge should not be gated on it. The 3.14 job
    # still reports its failure loudly in the Actions tab; it just does not
    # block. Promote it to blocking once it has been green for a while.
    name: Build & smoke-test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    continue-on-error: ${{ matrix.advisory || false }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
        include:
          - python-version: "3.14"
            advisory: true

    defaults:
      run:
        working-directory: OneDrive/Desktop/stock-valuation-predictor

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: OneDrive/Desktop/stock-valuation-predictor/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest flake8

      - name: Lint (flake8)
        run: python -m flake8 .

      - name: Byte-compile all sources
        run: |
          python -m py_compile app.py pages/home.py pages/research.py
          python -m compileall -q svp ci

      - name: Import the svp package
        run: python -c "import svp; print('svp', svp.__version__)"

      - name: Unit tests (pytest)
        env:
          # Keep the suite offline: the fallback paths are the point.
          HTTPS_PROXY: ""
          HTTP_PROXY: ""
        run: python -m pytest -q

      - name: Offline pipeline smoke test
        env:
          # Keep the test fully offline & deterministic — no external APIs hit.
          HTTPS_PROXY: ""
          HTTP_PROXY: ""
        run: python ci/smoke_test.py

      - name: Option-chain live code path (stubbed yfinance)
        env:
          # Runners have no reliable route to Yahoo, so the live branch is
          # driven against stubbed responses shaped like the real API rather
          # than hitting the network. ci/options_live_check.py covers the real
          # endpoint and is run manually.
          HTTPS_PROXY: ""
          HTTP_PROXY: ""
        run: python ci/options_live_path_test.py

      - name: Streamlit AppTest (renders all tabs, asserts no exceptions)
        run: python ci/apptest_check.py
